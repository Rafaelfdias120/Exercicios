#include <stdio.h>

int main(){

    int vl[10], i , cl;

    for ( i = 0; i < 10; i++){
        printf("Digite um valor: \n");
        scanf("%d", vl[i]);
    } 
    cl = cl + vl[i];
    printf("Resultado: %d", cl);

    return 0;
}